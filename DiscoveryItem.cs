// Create DiscoveryItem class to represent one type of discovery
namespace MotionGenerator
{
    public class DiscoveryItem
    {
        // Store the name of the discovery item
        public string? Name;

        // Store whether the discovery item was selected as missing
        public bool IsMissing = false;
    }
}
